const getters = {
  token: state => state.token,
  avatar: state => state.avatar,
  /*name: state => state.name,
  count: state => state.count,
  orgId: state => state.orgId,
  operatedLiveRoomId: state => state.operatedLiveRoomId,*/

};
export default getters
